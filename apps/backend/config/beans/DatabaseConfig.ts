export enum DatabaseDialectEnum {
  MY_SQL = 'MY_SQL',
  SQLITE = 'SQLITE',
  POSTGRES = 'POSTGRES',
}

export enum DatabaseDialect {
  MY_SQL = 'mysql',
  SQLITE = 'sqlite',
  // POSTGRES = 'postgres',
}

export enum DatabaseDialectDefaultPort {
  MY_SQL = 3306,
  SQLITE = 0,
  POSTGRES = 5432,
}
